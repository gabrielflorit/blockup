# blockup

```sh
Usage: blockup <command>

Commands:
  new    setup a new block
  serve  serve current block

Options:
  -h, --help  Show help                                                [boolean]
```
